# mr.bot

An AI automation bot powered by OpenClaw.

## Quick Setup

To set up OpenClaw for mr.bot, run:

```bash
./setup.sh
```

Or install directly:

```bash
curl -fsSL https://openclaw.ai/install.sh | bash
```

For detailed setup instructions, see [SETUP.md](SETUP.md).